{
  "name": "moderation-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that moderates Supabase inserts via Claude and posts flagged content to Slack.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250505.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.90.0"
  }
}
